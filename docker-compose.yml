version: '3'
services:
  web:
    build:
      context: .
    container_name: lyratech-web
    restart: always
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    networks:
      - frontend

networks:
  frontend:
    driver: bridge